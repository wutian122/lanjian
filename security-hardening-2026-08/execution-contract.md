# 执行契约：security-hardening-2026-08

## 已批准决策（DP-3，2026-08-19 老板确认）

- **工作流路径**：full（CLI 推荐：行为约束变更 + schema/API 变更 + 跨模块；老板「全部按推荐」确认）
- **范围**：11 项缺陷分三批实施（A1-A4 认证与越权 / B1-B4 功能正确性 / C1-C3 并发·SSRF·RBAC）；
  13 个存量失败测试修复（方案 A：仅测试侧）；finish_tool 并发树隔离。
- **执行模式**：Inline，逐批 TDD（先写失败测试 → 最小实现 → 全量回归）。
- **验证策略**：后端 `pytest` 全量 + `ruff`；前端 `pnpm type-check` + `pnpm build` + 静态断言。
- **红线**：不引入凭据字面量；SQL 一律参数绑定（Mimosa 约束）。

## 交付边界

- 后端 15 个文件 + 3 个新增测试文件（37 用例）；前端 9 个文件。
- 不改数据库结构；不做生产镜像构建与部署（另行执行）。
